# Python
.venv/
__pycache__/
*.pyc

# Node / frontend
node_modules/
dashboard/node_modules/
dashboard/dist/

# Claude Code machine-local settings (has absolute hook paths)
.claude/settings.local.json

# Offline map tiles (large, regenerable via go-pmtiles)
*.pmtiles

# OS
.DS_Store

# Flight data — runtime session logs (raw.log/CSVs), regenerable, can get large.
# Archive flights/ separately rather than committing per-run data.
flights/
